/** The object defining authentication keys for Rule34 API requests. */
export interface Authentication {
    /** The `user_id` parameter of the API key. */
    user_id: number;
    /** The `api_key` parameter of the API key. */
    api_key: string;
}
