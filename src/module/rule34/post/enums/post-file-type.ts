/** The possible animation states of a post. */
export enum PostFileType {
    /** A still image; no animation or audio. */
    Static,
    /** An animated, looped image; no playback control or audio. */
    Animated,
    /** An animated video. */
    Video
}
