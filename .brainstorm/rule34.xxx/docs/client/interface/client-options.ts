import {
    Authentication
} from "$root"

export declare interface ClientOptions {
    /** Authorization for API access. */
    auth: Authentication
    /** Client configuration object. */
    config?: {}
}
